package Samples;

import java.util.ArrayList;
import java.util.HashSet;
import java.util.Set;

public class JDemoOct17th {
    public static void main(String[] args) {
        String input = "abc";//abc,acb,bac,bca,cab,cba
        char[] myInputChars = input.toCharArray();
        String[] result = new String[10];
        for (int j=0;j<input.length();j++) {
            /*for (int i = 0; i < myInputChars.length; i++) {
                if (!(currChar == myInputChars[i])) {
                    result[i] = String.valueOf(currChar) + String.valueOf(myInputChars[i]);
                } else {
                    result[i] = "";
                }
                System.out.println("result ==> " + result[i]);
            }*/
        }
    }
}
